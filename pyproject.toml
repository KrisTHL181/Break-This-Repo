[project]
name = "break-this-repo"
version = "0.0.0"
description = "A shared workspace for experiments and Python utilities"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["linux-portability-lab"]
pythonpath = ["linux-portability-lab"]